# Configuração do Servidor
PORT=5000
NODE_ENV=development

# Configuração da Base de Dados PostgreSQL
# IMPORTANTE: Copie este ficheiro para .env e preencha com os dados da sua universidade
DB_HOST=localhost
DB_PORT=5432
DB_NAME=hr_database
DB_USER=seu_username
DB_PASSWORD=sua_password

# Opcional: String de conexão completa (alternativa às variáveis acima)
# DATABASE_URL=postgresql://username:password@host:port/database

# Segurança
# Para produção, gere uma chave secreta forte: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=sua_chave_secreta_aqui
